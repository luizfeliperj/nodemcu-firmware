# for i in {stdint,stdio,stdlib,string,types}.h; do sed -i -e "s,$i,c_\0," sqlite3.c ; done
# for i in config.h sqlite3.c sqlite3ext.h sqlite3.h; do cat x/sqlite-src-3190200/$i> $i; done
ac_cv_type_intptr_t=no \
ac_cv_type_uintptr_t=no \
ac_cv_func_fdatasync=no \
ac_cv_func_localtime_r=no \
ac_cv_func_malloc_usable_size=no \
	./configure \
		--disable-tcl \
		--disable-readline \
		--disable-largefile \
		--disable-threadsafe \
		--disable-load-extension \
		--enable-static \
		--disable-shared
